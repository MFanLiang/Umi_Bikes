import * as React from 'react';

interface IUserFilterFormProps {}

const UserFilterForm: React.FunctionComponent<IUserFilterFormProps> = (
  props,
) => {
  return <div className="UserFilterForm">用户过滤组件的</div>;
};

export default UserFilterForm;
